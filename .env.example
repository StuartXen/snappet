# Optional structured-vision endpoint. Leave unset for offline demo mode.
# POST JSON: { "imageKey": string, "species": "cat" | "dog" | "other", "imageUri"?: string }
# Response JSON: { "features": [{ "id": "earPosition", "rawScore": 0-2, "evidence": "..." }] }
EXPO_PUBLIC_VISION_ENDPOINT=
