# Optional structured-vision endpoint. Leave unset for offline demo mode.
# POST JSON: { "imageKey": string, "imageUri"?: string }
# Response JSON: {
#   "species"?: "cat" | "dog" | "other",
#   "speciesConfidence"?: 0-1,
#   "features": [{ "id": "earPosition", "rawScore": 0-2, "evidence": "..." }]
# }
EXPO_PUBLIC_VISION_ENDPOINT=
